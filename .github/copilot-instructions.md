# Copilot Instructions — Pokedoro

See [`CLAUDE.md`](../CLAUDE.md) at the repo root for architecture, commands, conventions, and testing guidance. It's the maintained source of truth for AI coding assistants working in this repo — keep this file as a pointer rather than duplicating its content, so the two can't drift out of sync again.
